# Email used by Let's Encrypt for certificate notices.
ACME_EMAIL=you@example.com

# Docker Desktop: reach a service running on the Mac host.
DROPVAULT_UPSTREAM=host.docker.internal:8080

# Portainer/VPS alternative when DropVault is on the shared Docker network:
# DROPVAULT_UPSTREAM=dropvault:8080
